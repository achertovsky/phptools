#!/bin/bash
path=./

if [ ! -z $1 ]
then
    path=$1
fi

phpcs $path
echo -e "(do not mind deprecated message, phpmd aware and gonna fix)"
phpmd $path text 'cleancode,codesize,controversial,design,naming,unusedcode' --exclude=vendor
phpstan analyze --level=7 $path
